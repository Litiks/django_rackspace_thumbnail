# -*- coding: utf-8 -*-
from setuptools import setup, find_packages

setup(
  name='django_rackspace_thumbnail',
  description='A simple django app to generate thumbnails for image files stored via django_cumulus',
  packages=['thumbnail'],
)
